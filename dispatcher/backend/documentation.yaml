openapi: 3.0.0
info:
  title: Zimfarm Dispatcher
  description: Zimfarm is a farm grow and harvest zim files
  version: 0.1.0
servers:
  - url: https://farm.openzim.org/api
paths:
  /workers:
    get:
      summary: List workers
      description: Optional extended description in CommonMark or HTML.
      responses:
        '200':    # status code
          description: A JSON array of user names
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string